<nav class="navbar">
  <ul class="nav">
    <li class=""><%= link_to 'WDI Boston Fall ''14', root_path %></li>
    <% if user_signed_in? %>
    <li class="user-signed-in align-right"><%= link_to current_user.email, '#',  class: 'dropdown-toggle', data: { toggle: 'dropdown' } %>
      <ul class="dropdown">
        <li class="align-right"><%= link_to('Logout', destroy_user_session_path, :method => :delete) %> </li>
      </ul>
    </li>
    <% else %>
    <li class="align-right"><%= link_to('Login', new_user_session_path)  %></li>
    <% end %>
  </ul>
</nav>
